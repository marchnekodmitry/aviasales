const initialState = {
  tickets: [],
  transferCount: 'all', // all, 0, 1, 2, 3
  sortType: 'cheap', // cheap or fast
  loading: false,
  error: false
};

const reducer = (state = initialState, action) => {
  switch (action.type) {

  }
};

export default reducer;